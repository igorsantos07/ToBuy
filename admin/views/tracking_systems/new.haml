.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:tracking_systems, :index)
      %li.active=link_to pat(:new), url(:tracking_systems, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:tracking_system)
    .inner
      -form_for :tracking_system, url(:tracking_systems, :create), :class => :form do |f|
        =partial "tracking_systems/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")